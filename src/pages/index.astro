---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import AppCard from '../components/AppCard.astro';
import ContactLinks from '../components/ContactLinks.astro';
import { SITE_TAGLINE, CONTACT_EMAIL } from '../consts';

const apps = (
  await getCollection('apps', ({ data }) => !data.draft && !data.unlisted)
).sort((a, b) => a.data.order - b.data.order);
---

<Base>
  <section class="pt-8 pb-12">
    <h1 class="max-w-xl text-3xl leading-[1.1] font-bold tracking-tight sm:text-4xl">
      {SITE_TAGLINE}
    </h1>
    <p class="mt-6 max-w-lg text-lg leading-relaxed font-medium text-muted">
      By day, I'm a QA lead and test automation engineer.
    </p>
    <p class="mt-3 max-w-lg text-lg leading-relaxed font-medium text-muted">
      I also build small, focused web apps and websites, mostly for groups of people
      trying to organize something together.
    </p>
  </section>

  <section id="apps" class="scroll-mt-8">
    <h2 class="mb-6 text-xs font-semibold tracking-[0.08em] text-muted uppercase">
      Apps
    </h2>

    {
      apps.length === 0 ? (
        <p class="text-muted">Nothing published yet.</p>
      ) : (
        <div class="grid gap-4 sm:grid-cols-2">
          {apps.map((app, i) => (
            <AppCard app={app} index={i} />
          ))}
        </div>
      )
    }
  </section>

  <section class="mt-20 rounded-xl border border-line bg-accent-soft/50 p-8">
    <h2 class="text-lg font-semibold tracking-tight">Working on something similar?</h2>
    <p class="mt-2 max-w-md text-[15px] leading-relaxed text-muted">
      I'm always happy to talk shop about these apps, or about building small
      software that people actually use, including something you'd like built.
    </p>
    <p class="mt-2 max-w-md text-[15px] leading-relaxed text-muted">
      I'm also open to QA engineering and test automation roles.
    </p>
    <div class="mt-5 flex flex-wrap items-center gap-x-4 gap-y-3">
      <a
        href={`mailto:${CONTACT_EMAIL}`}
        class="inline-flex items-center rounded-lg bg-ink px-4 py-2.5 text-sm font-medium text-paper transition-opacity hover:opacity-90"
      >
        Get in touch
      </a>
      <ContactLinks class="ml-2" />
    </div>
  </section>
</Base>
